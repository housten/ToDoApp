name: Run Tests

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  call-test-workflow:
    uses: housten/ToDoApp/.github/workflows/module-test.yaml@backtrack
    with:
      node-version: '20'
